# Công việc cần training:
- Học docker networking
- Lab (optional)
- Kubespray
- OpenFaas
- python for system

# Git
- cd "target"
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/Trourest186/K8s.git (Optional)
or git remote add origin git@github.com:<username>/<repo>.git
git push -u origin master

(*)
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
____
Using for linux to git repo

mkdir ~/.ssh
ssh-keygen
--> press enter for next informations

cat ~/.ssh/id_rsa.pub

copy key into Profile/Settings/SSH and GPG key, then create SSH new

continue -> (*)
